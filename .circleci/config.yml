version: 2.1
jobs:
  build:
    docker:
      - image: circleci/clojure:openjdk-8-tools-deps-1.10.0.411
    steps:
      - checkout
      - run:
          name: The First Step
          command: lein test
      - run:
          name: Code Has Arrived
          command: |
            ls -al
            echo '^^^That should look familiar^^^'